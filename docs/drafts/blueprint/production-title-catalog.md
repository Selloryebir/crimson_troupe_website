# 活页剧目标题目录蓝图草稿

- 文档类型：蓝图草稿
- 状态：候选，待人工审核
- 规划效力：无；仅供人工审核，不约束正式蓝图、源码或运行时内容
- 分析基线：人工提供并确认的 13 条活页剧目三语标题；当前 `BP-FND-DOMAIN`、`BP-CNT-CORE`、`BP-I18N-TERMS` 与 `src/data/productions.ts`
- 目的：完整保存活页剧目的中文、英文版本和日文版本标题正文，明确它们作为剧目策划、详情创作与多语言术语依据的使用边界
- 非目标：重新核验标题来源；根据标题自动补写并发布剧情；把标题记录直接视为已经排演的 `Production`；替换当前原创剧目

## 正文

### SET-01｜活页剧目三语标题目录

- 状态：待正式采纳
- 规划结果：保存 13 条活页剧目的中文、英文版本和日文版本标题正文；统一移除中文与日文包围书名号，不改变标题文字及语言版本差异
- 适用范围：剧目命名、详情文案策划、术语对应、搜索别名与来源定位
- 不包含：标题以外的剧情、类型、时长、主创、演出记录、世界归属或翻译推断
- 正式迁入目标：候选 `docs/references/production-title-reference.md`、`BP-I18N-TERMS`

#### SET-01 活页剧目标题表

`PROD-SRC-*` 只用于本草稿内引用，不是运行时 `productionId`。三列均只保存标题正文。`English` 是版本列名，不表示每个标题都由英语词汇组成；其中的法语、德语或混合形式保持原样。

| 来源 ID       | 中文       | English                   | 日本語             |
| ------------- | ---------- | ------------------------- | ------------------ |
| `PROD-SRC-01` | 凯旋颂     | Ode au Triomphe           | 凱旋の讃歌         |
| `PROD-SRC-02` | 湖中至宝   | Der Ring                  | 湖中の至宝         |
| `PROD-SRC-03` | 一百零一日 | One Hundred and One Days  | 百日一日物語       |
| `PROD-SRC-04` | 独行客     | Lone Wander               | 独り往く者         |
| `PROD-SRC-05` | 梦中奇缘   | Wonderland in Dream       | 夢の国の冒険譚     |
| `PROD-SRC-06` | 霜牡与雪牝 | Frost Deer and Snow Doe   | 霜の牡鹿と雪の牝鹿 |
| `PROD-SRC-07` | 赫里亚之辉 | Light of Heria            | ヘリアの輝き       |
| `PROD-SRC-08` | 七丘的狼母 | Sette colli's Mother Wolf | 七丘の母狼         |
| `PROD-SRC-09` | 初晓       | The Dawn                  | 曙光               |
| `PROD-SRC-10` | 金羽兽     | The Golden Fowlbeast      | 金色の羽獣         |
| `PROD-SRC-11` | 狂野之金   | Wild Gold                 | ワイルドゴールド   |
| `PROD-SRC-12` | 摇篮曲     | The Lullaby               | 子守り歌           |
| `PROD-SRC-13` | 欢欣鼓舞   | The Carnival              | カーニバル         |

#### SET-01 保存边界

- 三个标题字段是同一来源记录的并列版本，不通过显示名称生成稳定 ID；不追加包围书名号、机器翻译、罗马字、字面译名或替代标题；
- 活动名称和人工提供的来源说明保留在内部来源字段，不要求在沉浸式页面逐项显示来源标签；
- 正式迁移时，标题依据进入单一参考或术语来源；被实际采用的剧目再进入唯一运行时内容源，不能让参考表和 `src/data/productions.ts` 同时成为剧目事实源。

### SET-02｜标题参考与运行时 `Production` 的关系

- 状态：待正式采纳
- 规划结果：标题记录先作为可选剧目参考；只有经过内容选择、取得稳定 ID 并补齐必要字段的项目，才成为网站运行时 `Production`
- 适用范围：剧目策划、`Production` 数据、场次编排、列表与详情页、搜索和票务衔接
- 不包含：一次性把 13 条标题全部写入运行时、根据标题派生 URL、自动替换现有六个原创剧目或生成空详情页
- 正式迁入目标：`BP-CNT-CORE`、候选 `src/data/productions.ts`

#### SET-02 采用边界

- 被采用的标题必须获得不依赖任何语言标题的稳定 `productionId`，补齐经审核的剧目字段，再由一个或多个 `Performance` 引用；
- 未被采用的标题只作为文案、档案或历史剧目构思参考，不生成页面、搜索结果、票务入口或空 `Production`；
- 现有原创剧目不因本草稿自动删除、改名或失效，替换关系仍由 `OPEN-01` 决定；具体时间层归属由 `OPEN-02` 决定。

### SET-03｜三语标题与后续 i18n 的关系

- 状态：待正式采纳
- 规划结果：把中文、英文版本和日文版本名称作为三项已提供标题形式保存，但不把它们误写成当前九个国家版本的完整翻译覆盖
- 适用范围：术语数据、页面标题、搜索索引、跨国家版本对应和未来翻译范围清单
- 不包含：自动把英文版本名称同时批准为维多利亚与哥伦比亚发布内容，或用中文标题静默填充正式外语页面
- 正式迁入目标：`BP-I18N-TERMS`、`BP-CNT-CORE`

#### SET-03 语言映射规则

- 中文标题作为 `zh-CN` 来源形式，日文标题作为 `ja-JP` 来源形式；`English` 列先保存为未区分地域的英文版本来源形式 `en`；
- 维多利亚 `en-GB` 与哥伦比亚 `en-US` 可以在各自翻译开发中引用同一英文版本标题，但仍须分别纳入目标版本范围并通过人工审核，不能因存在 `en` 记录自动获得发布资格；
- 其他国家版本仍须建立各自批准标题。开发期缺失遵守“目标 locale → `zh-CN`”回退，正式发布不得保留回退标题；
- 中文、英文和日文标题字段均只保存标题正文，不包含 `《》`、`『』` 或其他包围书名号；组件不自动添加书名号，具体正文段落若因语法需要使用标点，由该段文案自行负责，不回写标题数据；
- 搜索索引只收录已经批准的标准形式和搜索别名。三语并列记录不授权一个国家版本跨语言返回其他版本内容。

### SET-04｜由标题扩写剧目详情的创作边界

- 状态：待正式采纳
- 规划结果：标题可以作为剧目概念和详情文案的创作起点，但所有扩写内容都作为概念站创作单独管理，不反向写入标题来源
- 适用范围：剧目简介、类型、宣传语、主创、时长、观演提示、视觉方向和历史演出文字
- 不包含：把智能体推演写成活动原文、要求访客侧逐段标注来源，或为尚未选用的全部标题批量生成低质量详情
- 正式迁入目标：`BP-CNT-CORE`；逐项创意候选进入 `docs/drafts/creative/`

#### SET-04 最小创作原则

- 只为确实服务当前页面或场次的标题创作完整内容，不为填满目录批量生成同质化剧目；
- 标题来源、人工提供的事实和本站扩写内容在作者侧保持可区分，访客侧仍遵守统一的沉浸式呈现与页脚声明；
- 标题暗示的地域、体裁、人物或剧情只能作为创意线索。除非另有依据，不写成活动已经确认的设定；
- 同一标题若被表站复排或被里站保存，可以共享稳定身份；只有具体版本确实存在不同内容时，才使用显式版本或关联字段，不复制两个无关 `Production`。

## 待解决问题

### OPEN-01｜标题参考池是否替换现有原创剧目

- 状态：已回答，待人工确认
- 所属规划或关联正文：`SET-02`、`SET-04`
- 问题：13 条活页剧目标题是补充候选池，还是要求后续替换当前 `src/data/productions.ts` 中的原创剧目？
- 影响：决定既有场次、剧目详情、搜索、票务和创意文案是否需要系统性重写
- 建议答案：人工澄清为补充候选池。现有原创剧目继续有效，本阶段不执行替换；后续只有人工明确启动相应阶段时，才根据当时的场次和内容目标建立有限迁移计划
- 合并动作：人工确认后将唯一结论融入 `SET-02`，同步修正实现影响并删除本问题

### OPEN-02｜活页剧目的时间层适用范围

- 状态：已回答，待人工确认
- 所属规划或关联正文：`SET-02`、`SET-04`
- 问题：这些标题是否默认属于旧剧团里站，还是允许表站复排、引用或重新制作？
- 影响：决定标题选择、详情口吻、`Performance` 关联、历史归属与跨时期稳定身份
- 建议答案：人工澄清为开发阶段保持时间层中立，不因来源活动自动限制为里站，也不自动进入表站；每条标题在正式版本中的存在方式、所属时间层和场次关联均由人工逐项决定
- 合并动作：人工确认后把时间层中立及逐项采用规则融入 `SET-02`，删除本问题

## 正式拆分与影响定位

| 来源 ID   | 目标位置                                                                                           | 影响性质   | 确认后的动作                                                                  |
| --------- | -------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------- |
| `SET-01`  | 候选 `docs/references/production-title-reference.md`、`docs/references/README.md`、`BP-I18N-TERMS` | 新增、修正 | 迁入已移除包围书名号的三语标题及来源边界；正式迁移后删除本草稿中的标题副本    |
| `SET-02`  | `BP-CNT-CORE`、候选 `src/data/productions.ts`                                                      | 修正       | 迁入参考标题成为运行时剧目的条件；仅在获得开发授权后添加被选中的 `Production` |
| `SET-03`  | `BP-I18N-TERMS`、`BP-CNT-CORE`                                                                     | 修正       | 迁入三语来源形式、地域英语审核和无包围书名号边界，不建立第二套语言发布门禁    |
| `SET-04`  | `BP-CNT-CORE`、候选 `docs/drafts/creative/`                                                        | 修正、新增 | 迁入详情扩写边界；实际选题另建有限创意草稿，不在正式蓝图复制逐项文案          |
| `OPEN-01` | `SET-02`、`src/data/productions.ts`、引用现有剧目的 `Performance`                                  | 候选影响   | 确认候选池或替换关系后决定是否需要迁移既有内容                                |
| `OPEN-02` | `SET-02`、`SET-04`、引用被采用剧目的 `Performance`                                                 | 候选影响   | 确认时间层规则后决定逐项内容归属，不在问题未关闭时批量编排                    |
