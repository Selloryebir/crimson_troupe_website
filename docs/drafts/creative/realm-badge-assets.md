# 国家版本徽章候选资产

- 文档类型：创意草稿
- 状态：已批准，等待代码切片消费
- 规划效力：不直接约束正式蓝图或源码；目录内文件不得作为运行时依赖
- 分析基线：`BP-I18N-CORE`、`docs/guides/realm-badge-authoring.md`、`docs/references/realm-badge-reference.md` 与本地九枚参考图
- 目的：在代码切片消费前保存已经批准的九枚单色再实现母版
- 非目标：扩大国家版本构建范围、提交本地参考图或在代码分支重新绘制另一套路径

## 正文

### CRE-01｜单色再实现母版

- 状态：已批准，等待迁入运行时资产目录
- 内容类型：透明 SVG 徽章资产
- 适用时间层：表站与里站共享国家版本导航
- 内容目的：以统一 `510 × 510` 正方形矢量画布和纯白前景重新建立九枚参考徽章的轮廓、重心、负形与缩小识别点
- 预期入口：正式采纳后由语言选择器以 CSS mask 和 `currentColor` 使用
- 设定边界：不提交或嵌入原始 WebP；轮廓提取只辅助定位，最终路径须经人工并排复核与清理；不为统一风格改变各实体核心设计
- 正式迁入目标：`src/assets/realm-badges/`

#### CRE-01 内容正文或叙事蓝本

候选文件位于 `docs/drafts/creative/realm-badge-assets/monochrome/`，文件名使用稳定 `editionId`。SVG 中没有文字、位图、外部依赖、脚本或背景；纯白只用于定义 alpha 轮廓，运行时颜色由页面 `currentColor` 决定。

#### CRE-01 素材与实现提示

- 并排检查一级轮廓、轴线、关键负形和附属结构，不以逐像素相等为目标。
- 在 `28px` 显示尺寸复核辨识度；高分辨率母版中的次级切口不得使小尺寸变成实心块。
- 正式迁移时只移动文件，不在代码分支重新绘制另一套路径。

## 待解决问题

无。资产的本阶段用途、迁移边界与运行时排除项已经由人工要求唯一确定。

## 正式拆分与影响定位

| 来源 ID  | 目标位置                   | 影响性质           | 确认后的动作                                             |
| -------- | -------------------------- | ------------------ | -------------------------------------------------------- |
| `CRE-01` | `src/assets/realm-badges/` | 新增运行时单色资产 | 与正式蓝图同步后原样迁入，核对组件实际消费并删除候选来源 |
