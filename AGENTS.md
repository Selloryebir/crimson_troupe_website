# Crimson Troupe Website Agent 规范

> 本文件适用于整个仓库，是智能体在本项目中进行读取、修改、验证和交付时的默认规范。用户当次明确指令优先于本文件。

## 1. 沟通与工作原则

- 所有开发沟通、计划、注释说明和交付报告默认使用简体中文；代码标识符保持现有英文风格。
- 先理解目标、现状和未提交变更，再实施满足需求的最小完整改动。只在歧义会实质改变结果时提问。
- 先搜索后新增，优先复用现有结构、样式变量和交互模式；不顺手重构，不为假设性需求引入框架、依赖或配置。
- 对已完成、已验证、未验证、受阻和仅为建议的内容作明确区分，不把静态检查说成浏览器验收。
- 默认不得 commit、push、创建或合并 PR、tag、部署或修改远端状态；只有用户明确授权时才能执行相应动作。

## 2. 项目目标与叙事边界

项目回答的问题是：“如果一位泰拉人搜索猩红剧团，会打开怎样的网站？”所有新增功能都应服务于可信的泰拉内体验，而不是普通作品展示页。

项目包含两个相互关联但责任不同的时间层：

- **表站 / 新剧团：** 高端、庄重、克制、专业，是一家真实运营中的巡演剧团官网。信息必须清晰、可信、可检索、可预约。
- **里站 / 旧剧团：** 诡异、邪恶、诱惑，表现为档案污染、时间异常和对观众身份的侵入，不只是表站的暗色换肤。

修改时必须保持以下边界：

- 表站先作为专业网站成立，彩蛋不能破坏正常浏览、键盘操作或退出路径。
- 里站可以制造不安，但不得使用真实欺骗、恶意下载、强制全屏、无法退出、闪烁滥用或收集隐私等手段。
- 两层共享品牌线索，但应有独立的信息结构、文案语气和交互反馈。
- 新增暗线时说明入口、触发条件、返回方式以及它对现有叙事的影响。

## 3. 内容、设定与素材权利

- 本项目是《明日方舟》世界观下的非官方同人概念站；不得暗示获得鹰角网络、Hypergryph 或其他权利方授权。
- 当前剧目、人物、机构细节和事务文案属于概念站原创设定，不得未经依据写成游戏官方事实。
- 不复制第三方网站或游戏的图片、视频、音频、字体、Logo、文案、代码、接口或独特页面构图。
- 可以借鉴抽象的信息架构、服务能力和交互原则，但必须完成泰拉化转译；新增重要参考时同步更新 `docs/references/`，记录来源、借鉴项、转译方式和排除项。
- 默认优先使用项目自有的 HTML/CSS 图形和明确拥有使用权的资产。引入任何外部资产前，必须确认来源、许可、署名要求和仓库存放方式。

## 4. 当前架构与文件职责

当前项目是使用 Astro 7、TypeScript 6 和 Node.js 24 LTS 的静态生成站点。完整目录与依赖规则以 `docs/architecture/repository-structure.md` 为准：

- `src/pages/`：文件路由和页面装配入口；
- `src/layouts/`：页面外壳、元数据和全局资源入口；
- `src/components/`：按表站、里站、浮层与共享职责拆分的 Astro 组件；
- `src/data/`：不依赖 DOM 的类型化结构内容；
- `src/scripts/main.ts`：原生客户端交互装配入口，功能按模块拆分；
- `src/styles/main.css`：样式装配入口，按基础、表站、里站、浮层和响应式分层；
- `src/assets/`：拥有使用权的运行时静态资产；
- `docs/`：架构、正式蓝图、临时草稿、指南和参考，不作为运行时输入；
- `README.md`：面向人员的仓库入口。

Astro 只负责构建期组件化和静态生成；客户端默认使用原生 TypeScript，不因局部交互引入 React、Vue 等运行时。模块通过显式 import/export 协作，不使用隐式全局状态，不复制稳定数据。

## 5. 蓝图驱动开发

`docs/blueprint/` 保存功能与体验的稳定目标、边界、状态和验收条件；它指导实现，但不复制源码、任务清单、讨论过程或测试日志。蓝图与源码的对应关系以 `docs/blueprint/traceability.json` 为唯一事实源。

`docs/blueprint/` 中的蓝图只允许 `active` 与 `retired` 两种状态：前者是当前有效约束，后者已经终止且不得继续约束源码。尚未采纳的新蓝图和修改稿只位于 `docs/drafts/blueprint/`；获人工采纳后默认以 `active` 新增或原位修正，除非人工明确要求终止。

`docs/drafts/` 保存没有正式产品效力的蓝图、建议与创意草稿，以及获准执行但完成后应删除的一次性开发计划。普通功能开发不得用草稿覆盖正式蓝图；只有任务涉及规划、草稿评审、已获授权的一次性计划，或用户明确要求正式迁移时才读取。智能体可以在 OPEN/TEST 项中分析并给出建议答案，但只有人工明确要求后才能将其融入草稿正文，且只有人工另行提出正式迁移请求后才能修改正式规划文件。迁移时遵循“写入目标、核对完整、删除草稿来源”的顺序，不保留正文副本；完整机制以 `docs/drafts/README.md` 为准。

- 修改 `src/` 下的功能源码前，先阅读 `docs/blueprint/README.md`，再运行 `npm run blueprint:where -- <源码路径...>` 并阅读返回的主要及相关蓝图；不要只依赖对话上下文猜测约束。
- 新增、删除或重命名 `.astro`、`.ts`、`.css` 功能源码时，同步维护追踪表；新建蓝图只用于能够独立演进的目标或能力，不为辅助函数、单个样式或临时任务建蓝图。
- 当修改改变用户目标、行为契约、状态归属、输入输出、失败路径、双时间层影响或 Demo/Formal 边界时，更新对应蓝图；纯内部重构、等价修复和不改变契约的文案或视觉微调不要求改写蓝图。
- 修改蓝图或功能契约时运行一次 `npm run blueprint:impact -- <蓝图 ID...>`。命令输出的是需要检查的候选影响范围，不是必须产生改动的文件清单；确认无实际影响时不得制造占位修改。
- 不在蓝图中积累审批问卷、重复摘要、决策流水账、审查记录或可以由 Git 与现有质量命令获得的信息。只有跨模块、难以逆转且会长期约束实现的决定才值得固化为蓝图契约。

蓝图相关具体格式、目录职责、示例和查询命令见 `docs/blueprint/README.md`；草稿格式、人工门禁、迁移和删除规则见 `docs/drafts/README.md`。这些规则与本文件冲突时以本文件及用户当次明确指令为准。

## 6. 体验与工程要求

- 支持现代桌面和窄屏浏览器，最低按 320px 宽度检查无横向内容丢失。
- 所有交互元素使用语义化按钮、链接、表单或对话框，并提供可理解的可访问名称、焦点行为和键盘路径。
- 新增动画必须遵守 `prefers-reduced-motion`；不得依赖动画才能理解信息或完成操作。
- 颜色、字号和装饰不能成为唯一的信息载体。内容提示、错误、筛选结果和世界状态应有文本反馈。
- 表单在接入真实后端前必须明确标注为概念交互，不发送个人信息，不伪造成功的远端操作。
- 默认不添加遥测、Cookie、远端请求、账号系统或第三方脚本。若需求涉及这些能力，先说明数据流、权限、失败路径和隐私影响。
- 保持无 JavaScript 时表站核心内容可读；JavaScript 失败不应留下覆盖全页且无法关闭的界面。

## 7. 默认工作流

1. 阅读 `README.md`、本文件及与任务直接相关的代码或文档；功能改动按第 5 节反查并阅读蓝图。
2. 检查当前分支和 `git status --short`，保留用户及其他智能体已有修改；不覆盖未知未跟踪文件。
3. 明确成功标准，搜索相关选择器、数据属性、锚点和调用关系。
4. 实施最小完整改动，同步处理桌面、窄屏、键盘、减少动态效果和双时间层影响。
5. 执行与风险相称的静态检查与浏览器验证。
6. 交付时列出实际修改、验证结果、未验证项、剩余风险及 Git 状态；不得声称未执行的 commit 或 push。

禁止使用 `git reset --hard`、`git clean -fdx`、宽泛 stash、历史重写、无条件 force push、通配删除或覆盖式恢复用户文件。需要清理智能体生成的临时文件时，必须使用精确路径并优先移动到可恢复的临时目录。

## 8. 验证基线

验证强度必须与实际改动风险相称。日常开发优先显式运行 `npm run quality -- <本次改动路径...>`；未提供路径时，该命令检查当前工作区相对 `HEAD` 的已暂存、未暂存和未跟踪文件，并只调度相关检查。不得为了统一形式对纯文档改动运行 Astro、ESLint、Stylelint、构建或浏览器验收。

- 草稿和普通说明文档：补丁空白与相关文件格式检查；
- 正式蓝图或追踪表：文档检查及一次 `blueprint:check`，契约变化时另按第 5 节运行一次 `blueprint:impact`；
- Astro/TypeScript：项目级类型检查、变更文件 ESLint 与格式检查；类型依赖图不能缩小为单文件检查；
- CSS：变更文件 Stylelint，以及与视觉风险相称的浏览器检查；
- 新增、删除或重命名功能源码：在对应源码检查之外增加一次 `blueprint:check`；
- 运行时源码影响构建产物时，在相关质量检查通过后运行一次 `npm run build`；该命令只构建，不隐式重复质量门禁；
- 工具链或质量配置变更、正式蓝图与运行时实现同时变更、运行时跨层集成、准备合并或发布、进入正式候选阶段：只运行一次 `npm run verify`。它依次执行一次 `quality:full` 和一次 `build`，不得在同一轮前后重复运行 `quality:full`、`quality` 或 `build`。

`npm run quality -- --plan <路径...>` 只显示检查计划，可用于确认没有无关全局检查。`quality:docs`、`quality:blueprint`、`quality:code`、`quality:styles` 和 `quality:full` 是需要显式扩大范围时使用的责任命令；日常检查不默认调用它们的合集。

构建后仅在运行时输出或用户体验可能变化时使用 `npm run preview -- --host 0.0.0.0`。视觉改动至少检查一个桌面视口和一个不大于 500px 的窄屏视口；交互改动按范围验证相关流程和可靠退出；无障碍改动验证键盘焦点、ARIA 状态与减少动态效果。纯文档任务不启动预览。

自动修复只使用仓库定义的 `npm run lint:code:fix`、`npm run lint:styles:fix` 或 `npm run format`，执行后必须检查 diff，并针对实际改动路径重新运行最小相关检查；只有达到上述完整门禁条件时才运行 `npm run verify`。若环境缺少浏览器或 JavaScript 运行时，应完成仍可执行的检查，并在交付中明确说明未验证部分，不以人工推断代替运行结果。未来调整正式工具链时，应在本节、`README.md` 与 `docs/guides/development.md` 同步记录唯一推荐命令。

## 9. Git 与提交规范

- 完整分支职责、同步方向与人工门禁以 `docs/guides/git-branch-workflow.md` 为准；执行任何 commit、push、创建合并请求或合并前必须先核对当前分支和该指南。
- `main` 是人工控制的稳定主线，只接受 `dev -> main`；未获明确要求不得 checkout 后操作或执行任何写入。创建 `dev -> main` 合并请求不授权批准或合并，最终必须等待人工审核确认。
- `dev` 只接收达到可部署、可浏览阶段里程碑的 `dev_code`，不承担日常功能开发；进入 `dev` 仍需人工明确授权。
- `dev_blueprint` 主动产生蓝图、设计、规划和文档治理改动，不主动修改 `src/` 的功能、设计或格式；可以运行只读源码检查，也可以通过合并接收 `dev_code` 的实现结果。
- `dev_code` 主动产生源码实现和非功能性源码修正；开发前同步已确认的 `dev_blueprint`，实现完成后按阶段同步回 `dev_blueprint`，形成候选里程碑前再吸收最新蓝图。
- 长期分支间使用 fast-forward 或正常 merge commit 保持共同祖先；不得使用 squash、历史重写或反复 cherry-pick 进行 `dev_blueprint`、`dev_code`、`dev` 与 `main` 的同步。
- 不修改现有分支跟踪关系，除非用户明确要求；不得仅凭当前分支名、无冲突或检查通过推定 push、合并或发布权限。
- 获得提交授权后，提交前重新检查完整 diff、未跟踪文件和验证结果，只纳入与任务有关的内容。
- 新提交使用 Conventional Commits 1.0.0：`<type>[optional scope][optional !]: <description>`。
- 功能使用 `feat`，缺陷修复使用 `fix`，仅文档使用 `docs`，构建或工具配置使用 `chore`；破坏性变更使用 `!` 或 `BREAKING CHANGE:` footer。
- 不得使用 `--no-verify` 绕过仓库钩子，不得把本地环境文件、秘密、浏览器截图或 Agent 临时目录提交入库。

## 10. 多智能体协作

- 只有任务可以独立完成、依赖清楚且写入范围不重叠时才并行；共享的页面布局、`foundations.css`、`main.ts` 装配顺序和同一文档应串行修改。
- 委派必须包含目标、精确文件范围、允许的写操作、验收条件和返回证据。实现智能体不得自行扩大范围或修改中央 Git 状态。
- 主智能体负责复核子任务 diff、整合冲突并重新运行关键验证；不得以子智能体的“完成”声明代替仓库级复验。
- Reviewer 应独立报告高置信问题及定位；若同一智能体直接修复了候选，则不得把自己的修复冒充独立审查结论。
- 交接只传递继续工作所需的信息：改动路径、关键决策、已运行检查、失败或未验证项、工作区状态和唯一下一步。
